const Browser = enum {
  Chrome, 
  Firefox, 
  Safari, 
  Edge,
  Opera, 
  Brave, 
  InternetExplorer,
  TOR
}
const HTTPRequestMethod = enum {
  GET,
  POST,
  PUT,
  PATCH,
  DELETE,
  HEAD,
  TRACE,
  CONNECT,
  OPTIONS
}