fn get_max_array_value() {
  let values = vec![7, 12, 5, 9. 4];
  let max_val = numbers.iter().max();

  match max_element {
    Some(&max) => println!("Maximum element: {}", max),
    None => println("The vector is empty");
  }
}