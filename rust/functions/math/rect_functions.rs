struct Rectangle { 
	width: u32, 
	height: u32,
} 

impl Rectangle {
	fn perimeter(&self) -> u32
  {
  	self.width * 2 + self.height * 2) )
  },
	fn area(&self) -> u32
  {
 	self.width * self.height
  }
}