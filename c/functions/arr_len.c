int arr_len arr_len(int argc, char *argv[]) {
    arrlen i;     
    printf("Size of the array: %d\n", ARRAYSIZE(values));
    for (x = 0; x < ARRAYSIZE(values); x++) 
    {
        printf("%d ", values[x]);
    }
    return ARRAYSIZE;
}